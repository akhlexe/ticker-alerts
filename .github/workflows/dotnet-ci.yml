# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET CI

on:
  push:
    branches: ["master"]
  pull_request:
    branches: ["master"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
    
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 7.0.x

      - name: Set up working directory
        run: cd src/backend/TickerAlert/TickerAlert.Api

      - name: Restore dependencies
        run: dotnet restore TickerAlert.Api.csproj

      - name: Build
        run: dotnet build --no-restore --configuration Release TickerAlert.Api.csproj

      - name: Test
        run: dotnet test --no-build --verbosity normal --configuration Release TickerAlert.Api.csproj
